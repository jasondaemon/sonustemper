PORT=8383
LOG_LEVEL=info
DATA_DIR=/data
IN_DIR=/data/in
OUT_DIR=/data/out
PRESET_DIR=/presets
SONUSTEMPER_TAG=edge

# API auth: set a long random key; UI will send X-API-Key automatically.
API_KEY=change-me-to-a-long-random-string
# Set to 1 ONLY to disable API auth (local dev); not recommended for LAN/remote.
#API_AUTH_DISABLED=0
