/* HTMX_PLACEHOLDER: Replace with official htmx.min.js (v1.9.x+) for native builds. */
/* This placeholder prevents native builds from silently shipping without HTMX. */
console.error("HTMX placeholder in use. Replace sonustemper-ui/app/static/vendor/htmx.min.js with the official minified build.");
